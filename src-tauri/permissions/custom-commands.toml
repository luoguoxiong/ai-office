# AI Office 自定义命令权限声明
# 需与 src/main.rs 中 generate_handler! 保持同步
[[permission]]
identifier = "allow-pick-workspace-dir"
description = "允许调用 pick_workspace_dir 命令(原生目录选择器,供前端「打开文件夹」按钮使用)"
commands = { allow = ["pick_workspace_dir"] }
